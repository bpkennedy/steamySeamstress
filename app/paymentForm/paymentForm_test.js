'use strict';

describe('myApp.paymentForm module', function() {

    beforeEach(module('myApp.paymentForm'));

    describe('paymentForm controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var paymentFormCtrl = $controller('PaymentFormCtrl');
            expect(paymentFormCtrl).toBeDefined();
        }));

    });
});
