'use strict';

describe('myApp.checkout module', function() {

  beforeEach(module('myApp.checkout'));

  describe('checkout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var checkoutCtrl = $controller('CheckoutCtrl');
      expect(cartCtrl).toBeDefined();
    }));

  });
});
