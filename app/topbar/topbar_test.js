'use strict';

describe('myApp.topbar module', function() {

    beforeEach(module('myApp.topbar'));

    describe('topbar controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var topbarCtrl = $controller('TopbarCtrl');
            expect(topbarCtrl).toBeDefined();
        }));

    });
});