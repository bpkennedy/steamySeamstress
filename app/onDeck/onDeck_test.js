'use strict';

describe('myApp.onDeck module', function() {

  beforeEach(module('myApp.onDeck'));

  describe('onDeck controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var onDeckCtrl = $controller('OnDeckCtrl');
      expect(onDeckCtrl).toBeDefined();
    }));

  });
});