'use strict';

describe('myApp.policies module', function() {

  beforeEach(module('myApp.policies'));

  describe('policies controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var policiesCtrl = $controller('PoliciesCtrl');
      expect(policiesCtrl).toBeDefined();
    }));

  });
});